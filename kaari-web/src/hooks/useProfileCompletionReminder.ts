/**
 * This hook previously showed a profile completion reminder
 * It has been disabled as requested by the owner
 */
export const useProfileCompletionReminder = () => {
  // The hook now does nothing, but we keep it to avoid breaking any imports
  return;
}; 